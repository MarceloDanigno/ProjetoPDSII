import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.needit.app',
  appName: 'Needit',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
